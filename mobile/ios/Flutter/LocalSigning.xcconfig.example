// Copy to LocalSigning.xcconfig (gitignored) or inject these as protected CI
// build settings. Never commit real team/profile/certificate values.
PAKPERK_DEVELOPMENT_TEAM = YOUR_TEAM_ID
CODE_SIGN_STYLE = Manual
CODE_SIGN_IDENTITY = Apple Distribution
PROVISIONING_PROFILE_SPECIFIER = Your Protected Profile
