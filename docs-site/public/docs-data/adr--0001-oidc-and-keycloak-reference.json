{"id":"adr--0001-oidc-and-keycloak-reference","html":"<p><strong>Status:</strong> Accepted — account verification and deletion administration implemented <strong>Date:</strong> 2026-07-31</p>\n<h2 id=\"context\">Context</h2>\n<p>Pakperk must retain guest reading while allowing users to own synchronized saves and participate in moderated public comments. The application must not collect or store passwords. Native clients also need a safe token lifecycle and account deletion must revoke and remove the upstream identity in the reference deployment.</p>\n<h2 id=\"decision\">Decision</h2>\n<p>Pakperk is an OpenID Connect native client and resource server. Mobile uses the Authorization Code flow with PKCE (S256) in the system browser. The API validates tokens using OIDC discovery and JWKS, including signature, allowed algorithm, issuer, audience, expiration/not-before, clock skew, and a required subject.</p>\n<p>Keycloak is the local, staging, and default self-hosted production reference provider. API authorization remains provider-neutral: verified <code>(issuer, subject)</code> values are mapped transactionally to a local user, and destructive provider actions are behind an <code>IdentityAdmin</code> adapter. Keycloak is the reference implementation target for that adapter; a no-op adapter is limited to explicitly incomplete tests/local flows.</p>\n<p>Only refresh tokens and minimum session metadata are persisted in platform secure storage. Access tokens remain in memory where practical. Pakperk does not copy email into its database by default.</p>\n<h2 id=\"implementation-status\">Implementation status</h2>\n<p>The provider-neutral verifier, publishable auth-readiness runtime, local account mapping, native AppAuth client, platform secure token store, reference Keycloak realm, and optional <code>GET</code>/<code>PATCH /v1/me</code> surface are integrated under <code>ACCOUNTS_ENABLED</code>. Guest paper routes are independent of OIDC readiness. The Phase 3 verification report records successful repository, native, live PostgreSQL, and real Keycloak/Mailpit acceptance runs.</p>\n<p>The provider-neutral identity-admin adapter now has a bounded Keycloak implementation loaded only by the dedicated deletion worker. Startup obtains a client-credentials token and proves its exact <code>manage-users</code> grant with non-destructive reserved-user reads; redirects, wrong status/shape, oversized responses, and missing privileges fail closed. Session revocation and identity deletion are idempotent steps in the leased deletion state machine. Credentials are owner-only files and provider coordinates are encrypted in the independently signed restore ledger. Production enablement still requires the exercised realm grant, secret rotation, alerts, independent backup topology, and restore replay.</p>\n<h2 id=\"consequences\">Consequences</h2>\n<ul>\n<li>Users can browse and read without an account; saves, comments, reports, blocks, and account settings require an authenticated local user.</li>\n<li>Mobile must implement secure storage, single-flight refresh, reauthentication, logout cleanup, and idempotency-aware retries.</li>\n<li>Backend configuration must include issuer, audience, allowed algorithms, discovery/JWKS timeouts, and a functioning identity-admin configuration when account deletion is enabled in production.</li>\n<li>Pakperk gains a local user record and public handle independent of provider display data, avoiding authorization based on client-supplied profile fields.</li>\n</ul>\n<h2 id=\"security-and-operational-implications\">Security and operational implications</h2>\n<ul>\n<li>Native clients are public: no embedded client secret, and redirect/post-logout URIs must be exact.</li>\n<li>Token contents, authorization headers, OIDC subject identifiers, and emails must be redacted from logs, telemetry, crash reports, and local general storage.</li>\n<li>Verification fails closed when required validation metadata is unavailable; unsigned tokens and token-selected algorithms are never accepted.</li>\n<li>Keycloak must enforce email verification for self-registration, brute-force protection, password recovery, short-lived access tokens, and protected refresh-token rotation/session policy.</li>\n<li>Account deletion disables the local account immediately, revokes upstream sessions, and then performs idempotent asynchronous erasure. Production startup must reject an enabled deletion feature without a working admin adapter.</li>\n</ul>\n","htmlZh":"<p><strong>状态：</strong> 已接受 — 已实现账户验证和删除管理 <strong>日期：</strong> 2026-07-31</p>\n<h2 id=\"context\">背景</h2>\n<p>Pakperk 必须保留访客阅读功能，同时允许用户拥有可同步的个人保存内容，并参与受内容审核约束的公开评论。应用不得收集或存储密码。原生客户端还需要安全的令牌生命周期；在参考部署中，删除账户必须撤销并移除上游身份。</p>\n<h2 id=\"decision\">决定</h2>\n<p>Pakperk 的移动端是 OpenID Connect 原生客户端，API 是资源服务器。移动端通过系统浏览器执行采用 PKCE（S256）的授权码流程。API 使用 OIDC 发现和 JWKS 验证令牌，检查内容包括签名、允许的算法、颁发者、受众、过期时间、“不得早于指定时间生效”（not-before）约束、时钟偏差容限，以及必需的主体标识。</p>\n<p>Keycloak 是本地开发环境、预发布环境和默认自托管生产环境所采用的参考身份提供商。API 的授权逻辑保持与身份提供商无关：经过验证的 <code>(issuer, subject)</code> 值会在一个事务中映射到本地用户，而针对身份提供商的破坏性操作均由 <code>IdentityAdmin</code> 适配器隔离。Keycloak 是该适配器的参考实现；no-op 适配器仅限用于明确标记为不完整的测试或本地流程。</p>\n<p>仅将刷新令牌和最低限度的会话元数据持久化到平台安全存储中。在可行情况下，访问令牌只保留在内存中。Pakperk 默认不会将电子邮件地址复制到自身数据库中。</p>\n<h2 id=\"implementation-status\">实现状态</h2>\n<p>与身份提供商无关的验证器、可发布认证就绪状态的运行时、本地账户映射、原生 AppAuth 客户端、平台安全令牌存储、参考 Keycloak Realm，以及可选的 <code>GET</code>/<code>PATCH /v1/me</code> 接口，均已集成并受 <code>ACCOUNTS_ENABLED</code> 控制。访客论文路由不依赖 OIDC 就绪状态。 第 3 阶段验证报告记录了针对代码仓库、原生客户端、实际 PostgreSQL 以及真实 Keycloak/Mailpit 的成功验收运行。</p>\n<p>与身份提供商无关的身份管理适配器现已提供一个具备边界限制的 Keycloak 实现，且仅由专用删除 worker 进程加载。启动时会获取客户端凭证令牌，并通过对预留用户执行非破坏性读取，验证其恰好具备 <code>manage-users</code> 授权；遇到重定向、错误的状态码或响应结构、超限响应或权限缺失时，均会失败并拒绝继续操作。会话撤销和身份删除是采用租约机制的删除状态机中的幂等步骤。凭证保存在仅限所有者访问的文件中，身份提供商坐标信息则在独立签名的恢复账本中加密。要在生产环境启用此功能，仍须实际验证 Realm 授权，并具备密钥轮换、告警、独立备份拓扑和恢复重放能力。</p>\n<h2 id=\"consequences\">后果</h2>\n<ul>\n<li>用户无需账户即可浏览和阅读；个人保存内容、评论、举报、屏蔽和账户设置则要求经过身份认证的本地用户。</li>\n<li>移动端必须实现安全存储、合并并发刷新请求、重新认证、注销清理，以及考虑幂等性的重试机制。</li>\n<li>后端配置必须包含颁发者、受众、允许的算法、OIDC 发现/JWKS 超时设置；在生产环境中启用账户删除功能时，还必须提供可正常工作的身份管理适配器配置。</li>\n<li>Pakperk 因此拥有独立于身份提供商显示资料的本地用户记录和公开用户名，从而避免根据客户端提供的资料字段作出授权决定。</li>\n</ul>\n<h2 id=\"security-and-operational-implications\">安全与运营影响</h2>\n<ul>\n<li>原生客户端属于公共客户端：不得嵌入客户端密钥，并且重定向 URI 与注销后重定向 URI 必须完全一致。</li>\n<li>令牌内容、Authorization 请求头、OIDC 主体标识和电子邮件地址必须在日志、遥测、崩溃报告和本地通用存储中脱敏。</li>\n<li>所需验证元数据不可用时，验证必须默认拒绝；绝不接受未签名令牌，也绝不接受由令牌自行选定的验证算法。</li>\n<li>Keycloak 必须强制执行自行注册所需的电子邮件验证、暴力破解防护、密码恢复、短期有效的访问令牌，以及受保护的刷新令牌轮换和会话策略。</li>\n<li>删除账户会立即禁用本地账户并撤销上游会话，然后执行幂等的异步擦除。在生产环境启动时，如果账户删除功能已启用，但身份管理适配器无法正常工作，则必须拒绝启动。</li>\n</ul>\n"}