{"id":"content-policy","html":"<p>Pakperk uses arXiv metadata and links every record back to its original arXiv page. Pakperk is not affiliated with or endorsed by arXiv.</p>\n<h2 id=\"production-v00-policy-status\">Production v0.0 policy status</h2>\n<p>The production migration now contains a feature-gated public paper-comment implementation. It includes authenticated posting, explicit Terms and Community Guidelines acceptance, normalized plain-text validation, distinct comment and user reporting, durable user blocking, audited moderation actions, shared rate limits, and a stable support contract. Reporting never creates a block or changes visibility. Both comment flags remain off by default. The repository now supplies account deletion, hosted policy/support, retention enforcement, telemetry, and operational controls, but public creation remains prohibited until those controls are exercised in the target environment and legal/store/moderation owners approve the evidence. This is an implemented dark-launch capability, not a claim that public UGC is live.</p>\n<p>When comments are enabled in a production release, comments are public user-generated content. They are not private notes or scholarly endorsements; the product discloses that fact and provides separate <strong>Report comment</strong>, <strong>Report user</strong>, <strong>Block user</strong>, support, and author-removal routes. <code>COMMENT_CREATION_ENABLED=false</code> stops only new posts so that reading and safety operations remain available. The authoritative product, moderation, retention, and deletion requirements are in <a href=\"/?doc=production-v0.0-plan\" data-doc-link=\"production-v0.0-plan\">the Production v0.0 plan</a> and <a href=\"/?doc=adr--0004-public-comments-and-moderation\" data-doc-link=\"adr--0004-public-comments-and-moderation\">ADR 0004</a>; the concrete wire and device behavior is in the <a href=\"/?doc=comments-and-moderation\" data-doc-link=\"comments-and-moderation\">comments and moderation contract</a>.</p>\n<p><code>FULLTEXT_POLICY</code> has two modes:</p>\n<ul>\n<li><code>prototype</code> is for local research and demonstration. A worker downloads a validated arXiv PDF to a random temporary path, never serves that PDF, parses it, and removes it after normalization.</li>\n<li><code>strict</code> publishes introduction/full-text-derived capabilities only when the cached paper license permits the configured reuse. Otherwise the paper remains metadata-only with an external arXiv link.</li>\n</ul>\n<p>The worker checks this policy before creating artifacts and the API checks it again every time derived content is served. The second check is intentional: switching a deployment from <code>prototype</code> to <code>strict</code> immediately masks old capabilities and denies persisted Introduction, Chat, and Connections data for unknown or unsupported licenses; a database cleanup is not required.</p>\n<p>Mobile builds have a corresponding compile-time switch:</p>\n<div class=\"sourceCode\" id=\"cb1\"><pre class=\"sourceCode sh\"><code class=\"sourceCode bash\"><span id=\"cb1-1\"><a href=\"#cb1-1\" aria-hidden=\"true\" tabindex=\"-1\"></a><span class=\"ex\">--dart-define=PAKPERK_FULLTEXT_POLICY=strict</span></span></code></pre></div>\n<p>Use it together with backend strict mode. When offline, a strict build retains metadata and original arXiv links but never falls back to device-cached or bundled derived content. A backend policy denial uses HTTP 403 with stable code <code>FULLTEXT_POLICY_DENIED</code> and is never treated as a transient fallback signal.</p>\n<p>Metadata, licenses, exact-ID lookups, and search results are cached. All legacy arXiv API requests pass through one database-backed cross-process gate with a minimum interval of three seconds by default and honor <code>Retry-After</code>.</p>\n<p>Before a public launch, the display and retention policy for derived full text requires review by qualified counsel. The prototype switch is not a legal determination.</p>\n","htmlZh":"<p>Pakperk 使用 arXiv 元数据，并为每条记录提供指向其原始 arXiv 页面的网址。Pakperk 与 arXiv 不存在隶属关系，也未获得 arXiv 赞助。</p>\n<h2 id=\"production-v00-policy-status\">生产版本 v0.0 政策状态</h2>\n<p>当前的生产迁移包含一套由功能开关控制的公开论文评论实现。它涵盖已认证发帖、明确接受《条款》和《社区指南》、规范化纯文本校验、彼此独立的评论举报与用户举报、持久化用户屏蔽、受审计的内容审核操作、共享限流，以及稳定的支持技术契约。举报绝不会自动创建屏蔽关系或改变可见性。创建评论默认保持关闭。仓库现已提供账户删除、托管政策及支持路由、保留政策执行、遥测和运营控制；但在目标环境中实际演练这些控制，并由法律、应用商店政策和内容审核负责人批准相关证据之前，仍禁止公开创建评论。这是一项已经实现但尚未公开启用的功能，不代表公开用户生成内容已经上线。</p>\n<p>在生产构建中启用评论后，评论属于公开的用户生成内容。它们既不是私密笔记，也不代表学术认可；产品会明确披露这一点，并分别提供 <strong>举报评论</strong>、<strong>举报用户</strong>、<strong>屏蔽用户</strong>、支持路由和作者自行移除内容的路由。 <code>COMMENT_CREATION_ENABLED=false</code> 只会停止发布新评论，确保读取和安全操作仍然可用。权威的产品、内容审核、保留及删除要求见 <a href=\"/?doc=production-v0.0-plan\" data-doc-link=\"production-v0.0-plan\">Production v0.0 计划</a>与 <a href=\"/?doc=adr--0004-public-comments-and-moderation\" data-doc-link=\"adr--0004-public-comments-and-moderation\">ADR 0004</a>；具体的网络和设备行为见 <a href=\"/?doc=comments-and-moderation\" data-doc-link=\"comments-and-moderation\">评论与内容审核技术契约</a>。</p>\n<p><code>FULLTEXT_POLICY</code> 有两种模式：</p>\n<ul>\n<li><code>prototype</code> 用于本地研究与演示。worker 把经过验证的 arXiv PDF 下载到随机临时路径；系统绝不对外提供该 PDF，并会在解析和规范化完成后将其删除。</li>\n<li><code>strict</code> 只有在缓存的论文许可证允许按配置方式复用内容时，才会提供引言或其他基于全文派生的功能。否则，论文只保留元数据以及指向 arXiv 的外部链接。</li>\n</ul>\n<p>worker 在创建产物前检查该政策；API 每次提供派生内容时都会再次检查。有意保留第二次检查，是为了让部署从 <code>prototype</code> 切换到 <code>strict</code> 后立即隐藏既有功能，并拒绝为许可证未知或不受支持的论文继续提供已持久化的引言、聊天与关联数据；无需清理数据库。</p>\n<p>移动端构建提供对应的编译期开关：</p>\n<div class=\"sourceCode\" id=\"cb1\"><pre class=\"sourceCode sh\"><code class=\"sourceCode bash\"><span id=\"cb1-1\"><a href=\"#cb1-1\" aria-hidden=\"true\" tabindex=\"-1\"></a><span class=\"ex\">--dart-define=PAKPERK_FULLTEXT_POLICY=strict</span></span></code></pre></div>\n<p>该开关应与后端的 strict 模式配套使用。离线时，strict 模式构建仍保留元数据和原始 arXiv 链接，但绝不会回退到设备缓存或应用包内的派生内容。后端因政策拒绝请求时，会返回 HTTP 403 和错误代码 <code>FULLTEXT_POLICY_DENIED</code>；客户端绝不能把它当作可临时降级的信号。</p>\n<p>系统会缓存元数据、许可证、按指定 ID 查询的结果以及搜索结果。所有旧版 arXiv API 调用都通过由数据库支持的跨进程门禁；默认最小请求间隔为三秒，并遵循 <code>Retry-After</code>。</p>\n<p>公开发布前，基于全文派生内容的展示和保留政策必须经过合格法律顾问审核。prototype 开关本身不构成法律判断。</p>\n"}