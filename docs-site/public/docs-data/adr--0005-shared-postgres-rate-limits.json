{"id":"adr--0005-shared-postgres-rate-limits","html":"<h2 id=\"adr-0005-shared-postgresql-rate-limits\">ADR 0005: Shared PostgreSQL rate limits</h2>\n<p><strong>Status:</strong> Accepted — implemented through Phase 6 <strong>Date:</strong> 2026-07-31</p>\n<h3 id=\"context\">Context</h3>\n<p>The demo's in-process limiting is not consistent when API replicas scale out. Authentication, saves, public comments, reports, profile mutation, deletion, and expensive paper operations require enforcement that is shared by every API instance. Introducing Redis or another network service for this milestone would violate the modular-monolith operational boundary without a demonstrated need.</p>\n<h3 id=\"decision\">Decision</h3>\n<p>Use PostgreSQL-backed shared rate-limit buckets, implemented as atomic <code>INSERT ... ON CONFLICT ... DO UPDATE</code> operations for fixed or sliding windows. PostgreSQL remains the source of truth alongside the existing leased jobs and synchronization records. Apply edge reverse-proxy limits as an additional coarse abuse control, not as the sole application limit. Return the stable <code>RATE_LIMITED</code> error and <code>Retry-After</code>.</p>\n<p>Initial buckets cover comment creation (user and keyed request origin), comment edits, reports (user, target, and keyed request origin), profile updates, library mutations, account deletion, and anonymous preparation/chat. The public preparation/chat limiter always consumes a keyed origin bucket and may also consume an anonymous-session bucket; it never relies only on the client-supplied session ID.</p>\n<h3 id=\"implementation-status\">Implementation status</h3>\n<p>Phase 3 adds the shared PostgreSQL bucket table, atomic fixed-window repository, bounded cleanup operation, and the first <code>profile_update</code> per-user bucket. A denied profile update maps to the stable <code>RATE_LIMITED</code> error and a delta-seconds <code>Retry-After</code> value. Phases 4–6 extend that foundation to library, comments/reports, account deletion, and preparation/chat. Public-operation and comment origin scopes use the shared trusted-proxy boundary and one generic, owner-only HMAC key; database failures fail these protected actions closed.</p>\n<h3 id=\"consequences\">Consequences</h3>\n<ul>\n<li>Rate-limit state is coherent across replicas and can be inspected, expired, tested, and backed up with the product database.</li>\n<li>New tables/indexes, a cleanup job, transactional integration, and metrics are required; traffic/load tests must validate contention and failure behavior.</li>\n<li>Limits become configuration with documented defaults and reviewed product policy rather than implicit per-process behavior.</li>\n<li>Redis, Kafka, NATS, and a separate rate-limit service are out of scope unless a future ADR changes this decision.</li>\n</ul>\n<h3 id=\"security-and-operational-implications\">Security and operational implications</h3>\n<ul>\n<li>IP/device scope keys are hashed with a rotating server secret when retained; raw identifiers are not logged or unnecessarily persisted.</li>\n<li>Buckets must have bounded retention and expiration cleanup to avoid unbounded growth. Monitor cleanup failures, denied-action rates, database contention, and bypass attempts.</li>\n<li>Database outage behavior must fail safely for sensitive writes and remain explicit in service health/runbooks; edge controls provide only supplemental protection.</li>\n<li>Limits are not authorization. Every protected operation still verifies the authenticated principal and applies ownership/status/terms checks.</li>\n</ul>\n","htmlZh":"<h2 id=\"adr-0005-共享-postgresql-限流\">ADR 0005: 共享 PostgreSQL 限流</h2>\n<p><strong>状态:</strong> 已接受 —— 通过 Phase 6 实现<br />\n<strong>日期:</strong> 2026-07-31</p>\n<h3 id=\"背景\">背景</h3>\n<p>演示中的进程内限流在 API 副本扩展时不够一致。<br />\n认证、保存、公开评论、报告、资料变更、删除以及昂贵的论文操作需要在所有 API 实例之间共享的限流。为这个里程碑引入 Redis 或其他网络服务将违反模块化单体的运营边界，除非有明确的需求证明。</p>\n<h3 id=\"决定\">决定</h3>\n<p>使用 PostgreSQL 支持的共享限流桶，通过原子的 <code>INSERT ... ON CONFLICT ... DO UPDATE</code> 操作实现固定窗口或滑动窗口。PostgreSQL 仍然是源真值，与现有的租约任务和同步记录并存。将边缘反向代理限流作为额外的粗粒度滥用控制，而不是唯一的应用限流。返回稳定的 <code>RATE_LIMITED</code> 错误和 <code>Retry-After</code>。</p>\n<p>初始桶覆盖评论创建（用户和键请求源）、评论编辑、报告（用户、目标和键请求源）、资料更新、库变更、账户删除和匿名准备/聊天。公共准备/聊天限流器始终消耗键源桶，也可能消耗匿名会话桶；它从不只依赖客户端提供的会话 ID。</p>\n<h3 id=\"实现状态\">实现状态</h3>\n<p>Phase 3 添加了共享 PostgreSQL 桶表、原子固定窗口存储库、有限的清理操作以及第一个 <code>profile_update</code> 每用户桶。被拒绝的资料更新映射到稳定的 <code>RATE_LIMITED</code> 错误和一个 delta 秒的 <code>Retry-After</code> 值。Phases 4–6 将该基础扩展到库、评论/报告、账户删除和准备/聊天。公共操作和评论源范围使用共享的可信代理边界和一个通用的、仅所有者使用的 HMAC 密钥；数据库故障将关闭这些受保护的操作。</p>\n<h3 id=\"后果\">后果</h3>\n<ul>\n<li>限流状态在副本之间保持一致，可以与产品数据库一起查看、过期、测试和备份。</li>\n<li>需要新的表/索引、清理任务、事务集成和指标；流量/负载测试必须验证竞争和故障行为。</li>\n<li>限流变为配置，具有文档化的默认值和审查的产品政策，而不是隐式的进程内行为。</li>\n<li>Redis、Kafka、NATS 和独立的限流服务不在范围之内，除非未来 ADR 改变此决定。</li>\n</ul>\n<h3 id=\"安全和运营影响\">安全和运营影响</h3>\n<ul>\n<li>IP/设备范围密钥在保留时使用旋转的服务器密钥进行哈希；原始标识符不会被记录或不必要的持久化。</li>\n<li>桶必须具有有限的保留期和清理过期，以避免无限制增长。监控清理失败、拒绝操作率、数据库竞争和绕过尝试。</li>\n<li>数据库故障行为必须对敏感写入安全失败，并在服务健康/运行手册中保持明确；边缘控制只提供补充保护。</li>\n<li>限流不是授权。每个受保护的操作仍然验证已认证的主体并应用所有权/状态/条款检查。</li>\n</ul>\n"}